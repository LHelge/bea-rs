name: Quality Checks

on:
  push:
    paths:
      - '**/*.rs'
      - 'Cargo.toml'
      - 'Cargo.lock'
  pull_request:
    paths:
      - '**/*.rs'
      - 'Cargo.toml'
      - 'Cargo.lock'

jobs:
  quality-checks:
    name: Format, Lint, and Test
    uses: ./.github/workflows/quality-checks.yml
